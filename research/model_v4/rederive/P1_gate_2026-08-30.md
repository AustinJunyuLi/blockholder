# P1 two-pass gate, 2026-08-30 — verdicts filed verbatim

Filed by the orchestrator before the ledger entry cites them. The gate ran over
`proofs/P1_proof.md` as amended by the 2026-08-29 substance batch (P1-R40-A items A1–A3, P1-R43,
P1-R44, P1-R45, per Austin's 2026-08-29 route ruling) and the 2026-08-30 gate repair round (five
items). Both agents were fresh: neither wrote the proof, the audits, or the repairs; the
re-deriver opened only `MODEL_CARD.md` and `CONTEXT.md`. This is the final gate over the theory
record — the freeze commit that carries this file closes the R-number sequence and makes
`threads/` archive.

---

## Pass 1 — adversarial proof-read (round 1 verdict, 2026-08-30)

PROOF-READ: PASS-WITH-REPAIRS

1. **WRONG (non-load-bearing, one-round repairable)** — P1_proof.md:1114–1121 (R45 boxed conclusion): "Under (i) and (ii), each $\mathcal T_i=c_i$ is continuous." The subsequence argument correctly proves $c_i(\cdot)$ continuous (the strict-to-non-strict limit step at :1118–1119 and the uniqueness invocation at :1119–1120 are both sound), but the identification $\mathcal T_i=c_i$ is never argued and is false under the file's own hypotheses. Witness (card-legal, satisfies (i), (ii), h.3, and h.6's bracket clause): $J=3$, $U_1\equiv0$, $U_2=s-1$, $U_3=2s-1$, bracket $[-2,2]$. Adjacent thresholds $c_1=1$, $c_2=0$ — both unique strict sign thresholds inside the bracket, so (ii) holds; argmax runs $1\to3$ at $s=\tfrac12$ (weakly increasing, h.3 holds); level sets $\{j^\star\ge2\}=\{j^\star\ge3\}=[\tfrac12,\infty)$ are nonempty up-sets with infima in the bracket, so Step 13's consumption form of h.6 holds. Yet $\mathcal T_1=\mathcal T_2=\tfrac12\neq c_1,c_2$ — plan 2 is skipped. The boxed argument cannot deliver $\mathcal T$'s continuity on skipped-plan/unordered-threshold menus; by card §8 rule 7 the unshown step cannot stand in. Repair: add to (ii) that the thresholds are weakly ordered across pairs (equivalently, no plan is argmax-absent everywhere), under which $\mathcal T_i=c_i$ does follow, or restate the claim as continuity of the $c_i$ alone. The theorem is untouched: P1 consumes h.6's continuity as an outright assumption (:1062–1079, 1137–1148) and (i)+(ii) are declared "candidate" (:1080–1082).

2. **MISCITED** — :1104–1106 vs NOTATION DELTA :1483–1516: R45's new symbols $d_i(s,k)$ and $c_i(k)$ have no NOTATION DELTA rows (card §8 rule 3). $c_i$ sits in §4.2's $c_j(s;\tau)$ crossing-date namespace; $d_i$ re-uses the calendar index $d$. Never blocks.

3. **MISCITED** — :664–666, 688–690: the envelopes' integrability is credited to "h.17-d's Gaussian tail and h.2's integrability clause"; what is actually used is Gaussian integrability of $|s|\varphi_s$ (h.17-d) with $L_j\le1$ — h.2's clause does not make $2\varphi_sL_j/\Lambda_k$ integrable. Mathematics unaffected. R44's envelope itself does follow from the step's own denominator bound, and the $\Lambda_k=0$ branch is substantively airtight.

4. **MISCITED (wording)** — :1058–1060: Step 14's parenthetical equates "adjacent pair with no crossing" with "a plan optimal nowhere, and with it every plan above it"; finding 1's own witness shows a plan can be optimal nowhere while every adjacent pair crosses, so the equivalence holds only in the canonical dominated-top case. Relatedly, the row's "A6 is read" bracket clause worded as "the indifference signal exists" is literally tangency-satisfiable while defeating the clause's purpose; Steps 13–14 consume the crossing form, which the row's purpose-clause forces — the proof never consumes more than the row grants. No mathematical consequence.

5. **UNCHECKED (observation)** — MODEL_CARD.md:651–655 evidence cell stale against the applied statement (R40-A clause described as unapplied).

6. **UNCHECKED (observation)** — :670–673: "exactly and $n$-free" holds as an a.e. density identity, not pointwise; the a.e. qualifier appears in the same sentence and nothing consumes the pointwise claim.

Everything else attacked held. R40-A is correct: under the level-set bracket clause the infima over the bracket equal the infima over $\mathbb R$, and $j_{\mathcal T(k)}$ agrees with $j^\star$ on all of $\mathbb R$ except the cutoff points (:998–1008); the $J=2$ dominated-plan witness (:1013–1023) and the lower-endpoint mirror (:1023–1025) genuinely violate the clause and are excluded, and NOT CLAIMED 16 (:1631–1635) is exactly that exclusion. R43 is sound: $\partial_P\varrho<0$ on all of $[A,\infty)$ at every $\pi\in[0,1]$ via R50's sharpened Step 7(iii) and $\bar m\ge m_0\ge0$, $\varrho$ jointly $C^1$, local IFT plus global uniqueness on $\mathbb R\times[0,1]$ give the joint-continuous root. The load-bearing chain closes: hypothesis match row↔proof clean in both directions; Step 4's scalar reduction, Step 6's Lusin–Souslin use, Step 9's reachability and two-case DCT with the R44 price convergence, Step 12's four-part lemma, Step 13's largest-weakly-increasing-selection construction, Steps 14–16's Brouwer assembly, and Step 17's a.s. consistency clauses all verify on the merits; zero hits for the §8 rule-7 vocabulary.

**Overall judgment.** Four-for-five on the merits with one real defect: R45's topological argument is internally correct but its conclusion asserts an identification a card-legal witness refutes — a wrong claim inside the candidate-sufficient-condition passage that the theorem's load-bearing path never consumes. All findings dischargeable in a single repair round without touching any hypothesis, step conclusion, or the statement of record.

---

## Pass 1 — retry verdict after the sanctioned repair round (2026-08-30)

RETRY: PASS

1. **Finding 1 (WRONG) — CLOSED.** Weak-ordering clause at :1143–1146; argued identification at :1157–1180; annotation at :1182–1192. On the merits: the clause's stated equivalence holds in both directions — adjacent disorder $c_{i+1}<c_i$ makes plan $i+1$ strictly beaten everywhere including at both threshold points, and under ordering each plan is argmax on its open threshold interval or inside the argmax at a collapse point. The counting-map argument delivers $\mathcal T_i=c_i$: off threshold points the counted indices form a prefix, the increments read $d_1..d_m>0$, $d_{m+1}..d_{J-1}<0$, the payoff sequence strictly unimodal, the argmax single-valued and equal to the counting map; at threshold points (i) forces $d_i=0$ and the argmax only widens across the tied block; the identification display consumes only the level-set infimum, which is tie-convention-insensitive. The continuity leg routes through the identification without circularity. One residual wording nit, not WRONG-class: "$j^\star$ is the counting map" (:1167–1168) is loose exactly at threshold points; the box scopes this "up to the threshold points themselves" (:1169) and nothing downstream consumes the pointwise value there. The annotation's witness (:1185–1189) is faithful.
2. **Finding 2 — CLOSED.** NOTATION DELTA rows at :1568–1569 with exactly the collision checks required.
3. **Finding 3 — CLOSED.** Both envelope credits reworded to Gaussian integrability of $|s|\varphi_s$/$\varphi_s$ (h.17-d) with $L_j\le1$, annotations at :679–685 and :710–714; the append-only P1-R20 row correctly untouched.
4. **Finding 4 first half — CLOSED.** Parenthetical at :1081–1085 narrowed to the canonical dominated-top case, dated note at :1087–1096.
5. **Finding 6 — CLOSED.** :692–695.

**Nothing else moved.** Line-shift accounting over the whole file: +14 (header) +10 (Step 9(b)) +10 (Step 14) +35 (Step 15) +2 (NOTATION DELTA) = +71, matching 1,784→1,855 exactly; every edit site carries the round label. Previously verified regions byte-unchanged. Banned-vocabulary sweep: zero hits.

**Judgment.** The one substantive repair — the weak-ordering clause plus the argued counting-map identification — is mathematically sound on the merits, closes the skipped-plan hole, and stays confined to the candidate-condition commentary with the theorem's load-bearing path untouched. The two-pass gate's adversarial proof-read leg is satisfied for the proof as amended by the 2026-08-29 substance batch and the 2026-08-30 repair round.

---

## Pass 2 — statements-only re-derivation (2026-08-30)

VERDICT: **PASS-WITH-CHANGES** (changes 1–2; none blocks — the amended statement supports its conclusion). Read only `MODEL_CARD.md` and `CONTEXT.md`; nothing under `proofs/`, `threads/`, `rederive/` opened.

Compressed hypothesis set as parsed from the row: (1) A1; (2) A2′; (3) A3; (4) A4; (5) A6 as read — (a) named tie-break-and-corner selection ⇒ $\mathcal T$ single-valued continuous $\Theta\to\Theta$, $\Theta$ nonempty per §4.5, (b) the bracket clause; (6) A7-J; (7) D1 by statement, hypotheses travelling (A5 discharge licensed by §5 lines 296–299); (8) §2 no-feedback timing with flag-terminates-the-pooled-round; (9) definitional round-2 action-set hypothesis; (10) continuation-cost equivalence (h.16); (11) $m_0\ge0$; (12) §4.3 $U_j$ definition with the timing convention; (13) consumed table restrictions. Supplementary, not existence: A8 + H-ord + upper-set engagement-flag; uniqueness not claimed.

Derived architecture (what the statement forces): Stage I at fixed $k$ — selection map → on-path pooled law (Bayes where $\Lambda_k>0$) → inner price layer (scalar strictly-decreasing map, unique root, continuous in $(\hat v,\pi)$) → flagged layer (point-mass belief at every image tuple) → off-path pooled layer (one full-support plan family at every $k$; $\kappa$-boundary nulls out of §3(vi) scope). Stage II — indifference signals exist (5b) + at most one crossing (A3) + monotone orientation ⇒ finite cutoff vector represents the best response on all of $\mathbb R$ and lies in $\Theta$ → selection single-valued, A6 continuity, Brouwer ⇒ $k^\star=\mathcal T(k^\star)$. Stage III — pooled sequential optimality via no-feedback timing; flagged sequential optimality at every flagged pair via price invariance (A7-J, $\pi=1$), order cancellation, h.16. A8 corollary; no uniqueness.

Findings: (1) the bracket clause is the right shape for F5 and a disambiguating reading of A6, coherent with the row's $\Theta$ content; (2) GAP — "the common bracket" was defined nowhere normative on the card → Change 1; (3) STALENESS — the evidence cell described the R40-A clause as unapplied → Change 2; (4) SCOPE — the clause excludes any menu/profile with a nonexistent adjacent indifference signal, including never-selected middle plans: stronger than F5 requires, card-legal, folded into Change 1's record; (5) no unused hypothesis (each maps to an architecture stage; the travelling-A5 tension discharged by $m_0\ge0$); (6) no §3/§4 contradiction (conclusion elements match §3(i)–(vi) one-for-one; the A4×action-set interaction closes the only potential off-image-tuple hole).

**Change 1** (statement cell, folded 2026-08-30): define the common bracket in the row — a fixed compact signal interval $[s_{\mathrm{lo}},s_{\mathrm{hi}}]$ at the given $\vartheta$, common in $k\in\Theta$ and in the adjacent plan pair, of which $\Theta$ is the ordered part — and record the domain narrowing (never-selected middle plans as well as dominated extreme plans), the stronger form taken so the cutoff vector carries interior data at every $k$.

**Change 2** (evidence cell, folded 2026-08-30 with one orchestrator conformance — the gate clause records both passes on file rather than the re-derivation alone): the R40-A clause is applied to the statement under Austin's ruling and gated by the fresh two-pass pair; R40-B not taken; F5 closed at antecedent level in the A($\tau$) pattern, with antecedent-satisfiability at the implemented calibration the open remainder.
