# Ticket 35 — Pass 1 (adversarial proof-read) findings, verbatim — 2026-08-25

Verdict: FAIL (1 FAIL finding). Findings 1–12 below, exactly as returned by the proof-reader.

**1. FAIL — `research/model_v4/proofs/P1_proof.md:502-503, 666` (Step 12 / Step 17(ii)): §3(ii) is discharged only at flagged nodes on the *selected* plan; flagged nodes reached by the blockholder's own date-0 deviation carry the requirement and no step meets it.**

Step 12 opens "let $j=j_k(s)$ **maximise** $U_\cdot(s;k)$ over $\mathcal J$ at a flagged $(j,s)$", and the entire contradiction runs off that maximality. But h.11 (`:103-108`) defines a round-2 action set $\mathcal Q_j(s)$ at **every** $(j,s)$ on the flagged set, so after a date-0 deviation to a non-selected plan $j$ that flags, the blockholder faces a genuine round-2 information set. PBE requires sequential rationality there. Step 17(ii) rests the whole flagged clause on Step 12 (`:666`), which never covers those nodes; nothing else in the file does.

Witness inside the row's own hypothesis set (so it is not excluded by A7-J or h.16): menu $\{$Exit, Hold, Voice-A, Voice-B$\}$ with A and B sharing a pooled path up to $f$ (hence $B^F_A=B^F_B$, $c_A=c_B$, $f_A=f_B$), $b^*_A,b^*_B$ continuous strictly increasing with disjoint ranges (A7-J holds — tuples separate through the sum coordinate), $C_A(s)=C_B(s)$ (h.16 holds), and $G_B(s;k)-G_A(s;k)=\delta>0$ for all $s$, with $E_A=E_B$ large enough that neither A nor B is ever selected. At the node "type $s$ played A's pooled path, flag landed", the specified continuation $Q^F_A(s)$ is strictly beaten by $Q^F_B(s)\in\mathcal Q_A(s)$ by $\delta$. The assembled object satisfies §3 items (i), (iii)–(vi) and fails (ii) at a flagged node — the same failure mode as WHERE IT FAILS 7 and as the 2026-08-23 demotion, one node-class over.

Bounded repair path (it exists, but it is not in the file and it changes the constructed object): specify the flagged component as the class-argmax, $Q^F_{j^\dagger}$ with $j^\dagger\in\arg\max_{j'\in\mathrm{class}(j)}G_{j'}(s;k)$. Within a class $E$ is constant (shared pooled path) and $C$ is constant (h.16), so $\tilde U_j:=\max_{j'\in\mathrm{class}(j)}G_{j'}-C_j-E_j=\max_{j'\in\mathrm{class}(j)}U_{j'}$, hence $\max_j\tilde U_j=\max_j U_j$ and the date-0 argmax is unchanged; Step 12 already gives $G_j\ge G_{j'}$ at the selected $j$, so on path the two continuations coincide; the deviation tuple $(B^F_j(s),Q^F_{j^\dagger}(s),1)$ is in the image of the flagged-pair map, so Step 10's pinning still applies with the *correct* signal $s$. Note h.16 is load-bearing for this fix too — without it $\tilde U_j\ne\max_{\mathrm{class}}U$ and date-0 optimality is not preserved. Tagged FAIL rather than REPAIR because the row's clause "a sequentially optimal flagged component" currently has no supporting argument at these nodes, and the fix modifies the equilibrium object and adds a lemma the file lacks. Vacuous on the pinned single-Voice menu (class is a singleton), exactly co-extensive with h.16's bite.

**2. REPAIR — `P1_proof.md:57-59` vs `MODEL_CARD.md:156-167`, `:324` (h.2 = A2, not A2′).** The row states **A2′**; h.2 states A2 with "prices and payoffs **bounded** on the maintained parameter set", the clause the card declares **false** and "inconsistent with the rest of the card" ($v$ Gaussian, flagged region unbounded in $s$). Proof-vs-row hypothesis mismatch of the same species as the A7 mismatch that caused the demotion, and a hypothesis set containing a card-declared-false clause proves the row vacuously rather than validly. Fix is determinate and every use survives: Step 3 and Step 9 use only the finiteness clauses (in A2′); Step 13's "finite **and bounded** by h.2" (`:575`) needs only finiteness of the menu plus finiteness of each $U_j(s;k)$, supplied by A2′'s local-boundedness-in-$(s,\vartheta)$ and $\mathbb E[\max_j|U_j|]<\infty$. P1-R15's staleness sweep against `d2ccf62` (`:1030`) brought LABEL CLAIMED and Step 14 to stamp but missed this.

**3. REPAIR — `P1_proof.md:66-71`, `:298-301`, `:332-337`, `:614-615` vs the row's "**A5 is not assumed**" (`MODEL_CARD.md:324`). Ruling: the mismatch is real but eliminable inside the file.** The row disclaims A5 wholesale while justifying only its existence/uniqueness half; the proof carries h.5 = A5 as a numbered hypothesis and consumes it at three places. Adjudication use by use: (a) Step 5(a)'s "h.5 supplies a unique fixed point" is derived at Step 7 from h.12 ($m_0\ge0$), which the row does list; (b) Step 6(b)'s single-valuedness of $\mathcal G_F$ likewise from Step 7(iii), and its **continuity in the belief** — the one genuinely load-bearing A5 use, since Step 6(d)'s Borel-ness of $P^F(\cdot;k)$ is "Borel $\circ$ continuous" — is derivable from Step 8's implicit-function argument ($\varrho\in C^1$ jointly in $(P,\hat v)$, $\partial_P\varrho<0$ strictly at every root by Step 7(iii)); Steps 7–8 do not depend on Step 6, so re-citing creates no circularity; (c) Step 15's "by h.5 the inner prices are continuous in the cutoffs" is **non-load-bearing**, because the proof's actual route to continuity of $\mathcal T$ is h.6 asserting it outright (Step 16, `:655`). Fix: delete h.5 from the hypothesis list, re-cite Step 5(a)/6(b) to Steps 7–8, and mark Step 15's clause as commentary.

**4. REPAIR — `P1_proof.md:420-433` (Step 9(b) establishes convergence of the *plan* posterior only; the price at a reachable but $k$-null pooled history needs the joint $(j,s)$ posterior).** Step 4 shows the pricing map depends on $\mathcal I$ through the two scalars $(\hat v,\pi)$. The perturbation limit as written is "the perturbed posterior **over plans**" — a ratio of degree-1 polynomials in $1/n$ with the signal integrated out (see the displayed denominator, `:425-427`). That delivers $\pi(\mathcal I)$ but **not** $\hat v(\mathcal I)=\mathbb E[v\mid\mathcal I]$, which is a functional of the *signal* posterior. This is load-bearing: Step 13 evaluates $U_j(s;k)$ for **every** $j\in\mathcal J$, including plans carrying zero probability under $k$ (collapsed regions), whose pooled-execution bracket reads prices at $k$-null histories. Fix is determinate and changes nothing in the object: run the same ratio argument on the joint density $w_n(j\mid s)\Pr(\mathcal H^P_d\mid j,s)\,\varphi_s(s)$, which converges pointwise in $s$, and pass to $\mathbb E[Y\mid\cdot]$ by dominated convergence under A2′'s integrability.

**5. REPAIR — `P1_proof.md:456-460` (Step 10: "that pair has strictly positive weight" is a positive-probability argument applied to a null event).** $s$ is Gaussian, so the pair $(j,s)$ has probability zero under every stage-$n$ perturbation; what has positive weight is the *plan* conditional on the type. "The perturbed posterior at $\sigma_F$" is therefore a conditional on a null event and is defined only up to null sets, so the sentence does not establish the claim at *every* image tuple. The conclusion survives: injectivity gives a genuine pointwise map $\iota_F$, so $\delta_{\iota_F(\sigma_F)}$ is a valid version of the conditional law at every $n$ and its limit is itself — enough for an existence theorem. Fix: state the version explicitly. Related OBSERVATION: the CLAIM's and row's "**pinned**, not chosen, at every tuple" overstates pointwise forcing — any a.e.-equal selection also satisfies §3(vi) — and `proofs/A7_construction.md:387-388` hedges precisely this ("anything about the conditional law on off-path *tuples* (pinned only up to null sets)").

**6. REPAIR — `P1_proof.md:435-442` (Step 9(c)'s "no step consumes an unreachable history" holds for a.e. $s$, not every $s$).** Reachability (`:409-413`) requires a **positive-probability** signal set $S$. The joint mark-and-flag vector $\big((q_{jd'}(s))_{d'\le d},\mathbf 1\{f_j(s)\le d\}\big)$ takes finitely many values (h.2), so its level sets partition $\mathbb R$ into finitely many Borel cells — but a particular cell may be $\Phi_s$-null. At such an $s$, plan $j$'s own realised pooled histories are unreachable and Step 11's bracket reads an undefined price. Fix: either restrict the claim to a.e. $s$, or assign arbitrary prices at unreachable histories (§3 constrains nothing there, per NOT CLAIMED 13) so $U_j(s;k)$ is defined everywhere.

**7. REPAIR — `P1_proof.md:668-669` and NOT CLAIMED 8 (`:946-948`): "at the cutoff points the two adjacent plans are indifferent (Step 13's construction)" is not supported by Step 13's construction.** $\mathcal T_i(k)=\inf\{s:j^\star(s;k)\ge i+1\}$ need not attain its infimum, so at $s=k^\star_i$ one can have $j^\star(k^\star_i)\le i$ while $j_{k^\star}(k^\star_i)\ge i+1$ (Step 1's $\le$ convention counts the cutoff). Indifference at that point requires continuity of $s\mapsto U_j(s;k)$, which is Step 15(i) — explicitly **not** a hypothesis and explicitly not derived (NOT CLAIMED 11, `:954-958`), and WHERE IT FAILS 4 exhibits a card-legal plan making $U_j(\cdot;k)$ jump. Fix: take the equilibrium plan map to be $j^\star(\cdot;k^\star)$ (optimal at every $s$ by construction, weakly increasing, represented by the same $k^\star$ under the strict-inequality tie convention, which §3(i) does not pin), rather than $j_{k^\star}$.

**8. REPAIR — `P1_proof.md:233-234` (Step 2's Borel justification is false for Exit).** "Measurability in $s$: $B_j(\cdot,d)$ is monotone by card §4.2, hence Borel" — card §4.2 (`MODEL_CARD.md:81`) imposes $\partial_sB_j\ge0$ for **Voice** only; Exit is "weakly decreasing" **in $d$**, and the card supplies Borel-in-$s$ for Exit as a separate explicit clause, calling it "a genuine addition for Exit". This is load-bearing (Step 9's reachability integrals and pooled pricing integrate over all types including Exit). Fix: cite the §4.2 Borel clause, which reaches P1 through "D1 by statement" and the §4.2 table restrictions the row lists.

**9. REPAIR — `P1_proof.md:98-102` (h.10 omits the flag-terminates-the-pooled-round clause that the row lists and Step 11 consumes).** The row's hypothesis is "the §2 no-feedback timing **read with the flag-terminates-the-pooled-round clause**"; h.10 transcribes only the no-feedback half. Step 11's decomposition (`:490`) sums the pooled execution over $d\le f_j$ and treats $Q^F_j=b^*_j-B^F_j$ as the whole residual position — both are the flag-terminates clause, not the no-feedback clause. Card §8 rule 6 requires each hypothesis enumerated and used. Fix: append the clause to h.10 and cite it at Step 11.

**10. OBSERVATION — `P1_proof.md:103-108`: h.11's action set is *type-indexed*, which is what makes Step 12's date-0 pushback work at all.** $\mathcal Q_j(s)=\{Q^F_{j'}(s)\}$ evaluates the menu at the **true** $s$, so the classic mimicry deviation "submit the order type $s''\ne s$ would submit" is excluded by fiat, even though that order is a real number in $[0,\bar b-B^F]$. Step 12's contradiction depends on this: under mimicry the tuple would be $\sigma_F(j',s'')$, priced at $\mathcal G_F(\hat v(s''))$, so the deviation payoff is not $G_{j'}(s)$ and the move back to date 0 fails. Step 12's converse (`:546-556`) and WHERE IT FAILS 2 discuss only the full-interval alternative and never this intermediate. The row owns h.11 descriptively and card §9 item 2 records that the IC burden is unsettled, so this is non-blocking — but it is the sense in which "sequential optimality of the flagged component" is obtained definitionally.

**11. OBSERVATION — CLAIM (`P1_proof.md:19`) says "Under hypotheses h.1–h.12, h.14 and h.16", which sweeps in h.8 = A8**, an assumption the claim's first half does not use (h.8's own line says "*Used: Step 19 only*"). Also `:59`'s "*Used: Steps 3, 9, 13, 16*" lists Step 16, which cites h.2 nowhere.

**12. OBSERVATION — Step 5's off-path pooled prices are never assembled at Step 17(iv) (`:673-675`)**, which evaluates prices "at the beliefs of (iii)" — i.e. the on-path Bayes beliefs — while Step 5 (`:318-319`) defines the family only "on those histories that carry positive probability under the conjecture $k$" and defers the rest to Step 9. §3(iv) at reachable $k$-null histories should be stated as the inner fixed point at Step 9's limit belief.

**Repair-table check (P1-R9…R16):** every entry describes an edit actually present. Nit: R16 is filed between R12 and R13 (`:1027`). Conclusion strength matches the row in both directions — no silent weakening.

**Overall verdict: FAIL (1 FAIL finding)** — finding 1 blocks the label move; its repair path is bounded and named, and it is vacuous on the paper's pinned single-Voice menu.
