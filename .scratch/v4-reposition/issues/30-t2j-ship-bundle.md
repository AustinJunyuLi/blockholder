# 30 — T2j · Ship: model note, two-round sign handoff, GPT Pro end bundle

**Lane:** theory (`v4-theory` worktree at `~/Projects/blockholder_v4_theory`)

**Routing (lane v2, agentic):** Opus for the model note; Sonnet for LaTeX
plumbing; **Fable does the final coherence read of the whole stack** before
the bundle is declared paste-ready (a named Fable checkpoint). This ticket
contains the lane's ONE planned GPT Pro courier moment.

**Premise:** tickets 21–29 landed: proofs on file, labels moved per evidence,
check JSONs committed. `research/model_v4/HANDOFF_sign.md` exists in its
provisional repo-model form (ticket 05).

**What to build:**

- [ ] `research/model_v4/model_v4.tex` + `model_v4.md` mirror (ticket 06's
      deliverable): primitives, timing, partition, equilibrium notion, the
      theorem stack with honesty labels exactly as the ledger has them, and
      the draft_v2 reuse/simplify/drop map; compiles with xelatex
- [ ] `HANDOFF_sign.md` updated with the TWO-ROUND number (sign, magnitude
      with units, condition/region, model, date, commit) — the earlier
      repo-model entry kept visible; pushed immediately (the empirics lane
      pulls this file)
- [ ] The GPT Pro end bundle assembled as ONE paste-ready file
      `research/model_v4/threads/gpt_end_review_bundle.md`: current card +
      all proofs + raw check output (verbatim JSONs) + the ask (adversarial
      review; verdicts WRONG/MISCITED/UNCHECKED per claim; demotions only) —
      Austin pastes it once into a fresh GPT Pro thread; the answer is
      ingested by the courier discipline (verbatim save + audit)
- [ ] Ticket 08 (sections_v3 model/theorem/proofs .tex) noted as next,
      blocked on the GPT review outcome
- [ ] Session log close-out; commits on `v4-theory`

**Blocked by:** 27, 28, 29 (and 05 for the provisional handoff entry).

**Status:** ready-for-agent

## Comments
