# 26 — T2f · T1 proof: the partition attenuation theorem

**Lane:** theory (`v4-theory` worktree at `~/Projects/blockholder_v4_theory`)

**Routing (lane v2, agentic):** one Opus writer agent. May cite D1, L1, L2, L3,
L4 by their card-ledger statements; must not read their proof files.

**Premise:** card ledger statement of T1; turn-1 statement + check request in
`threads/thread1_turn1_answer.md` §T1 (including the equivalent local window
condition ∂_{r_T}𝒮_P/𝒮_P ≤ Ω_{r_T}/(1−Ω)). The O-1 finding is binding: no
global window sign — the window conclusion is an iff between the weight and
composition effects.

**What to build:** `research/model_v4/proofs/T1_proof.md`, answer template,
card notation binding.

- [ ] 𝒮 = (1−Ω)𝒮_P derived from L1 + L2 at fixed policies (each citation
      explicit; Ω fixed w.r.t. κ stated as the PE hypothesis it is)
- [ ] Threshold margin: attenuation via L4, with the W_τ·C_τ ≤ 1 product form
- [ ] Window margin: the exact iff (W_T·C_T ≤ 1) and its local form proved
      equivalent; NO unconditional window sign claimed
- [ ] The old framework's numbers (ratios ≈1.06/1.19/1.14 above, 0.38 at mass
      0.50) named in WHERE IT FAILS as the live failure case for an
      unconditional window theorem
- [ ] Session log line; commit on `v4-theory` (label stays CONJECTURE)

**Blocked by:** 21 (L3), 22 (L4) — statements finalized there; if their
statements survive unchanged from the card ledger, this can start as soon as
those tickets confirm the statements.

**Status:** ready-for-agent

## Comments
