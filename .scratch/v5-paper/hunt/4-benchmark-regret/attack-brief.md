# Judge brief (the prompt sent to the judge)

You are the independent judge of a numerical record in a theory hunt for the paper in the git worktree /Users/austinli/Projects/blockholder_v5. Work from that directory. You did not write the record. Read, in this order: /Users/austinli/Projects/blockholder_v5/CLAUDE.md, /Users/austinli/Projects/blockholder_v5/CONTEXT.md, /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/README.md (the compute-lock rules bind you too), the brief at /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/brief.md, the memo at /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/memo.md, the script /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/regret.py and the record /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/regret.json. Then read numerical_v4/policy.py (plan_payoff, engagement_cost), numerical_v4/menu.py (breakpoints, atoms, b_star, n_days, legal_clock, stake_path), numerical_v4/flagged.py (flagged_price_at) and numerical_v4/pooled.py (pooled_pass, inner_price). Your job is to decide whether the record's bounds are certified, that is proved to bound the essential supremum of the regret R(s) = max_j U_j(s) - U_{j(s)}(s) at the benchmark policy at each node, as the memo claims. Attack the certification: (1) do the closed-form branch payoffs in the memo and script equal plan_payoff exactly on each piece, including the flagged Voice branch and the trade-cost sums; (2) are the derivative bounds (equations 3 to 7 of the memo) valid on the closure of each piece, including the implicit-differentiation bound on the flagged price and the bound on |P_F|; (3) is the breakpoint set complete, so that no piece hides a change of assigned plan, of n(s), of the crossing date or of the filing status, and are the support endpoints and the cutoffs handled at the closure correctly; (4) is the cover inequality (8) applied with the right radius and does the record's float allowance cover the residuals; (5) does the script compare all three plans, not only adjacent ones. Recompute one node with one pooled pass under the compute-lock rules (check that /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/runs/COMPUTE_LOCK is absent and `pgrep -f numerical_v4` is empty, write the lock with pid, what, started, owner, delete it in a finally block; the pass is about 10 seconds and 6 GB; run nothing heavier, no solve, no script under numerical_v4/checks/, no smoke). At that node compare the script's branch payoffs with plan_payoff at at least 50 interior points per piece to 1e-12, check the sample maximum on the attaining piece, and check the Lipschitz constants against finite differences of the gap along the piece. Write your own script under /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/ (name it judge_*.py) and run it from the repository root as PYTHONPATH=. .venv/bin/python <script>. Return PASS only if the bounds are certified as claimed at every node and your one-node recompute agrees; FAIL with the precise hole otherwise; nits that are not holes after the word 'Nits:'. Do not edit the memo, the script or the record. Run no git command. Write nothing except /Users/austinli/Projects/blockholder_v5/.scratch/v5-paper/hunt/4-benchmark-regret/attack.md (plain prose, no em dashes, sentence-case headings, ending with a VERDICT JSON block of the form {"verdict": "PASS" | "FAIL", "reasons": str}) and your judge script beside it. Then reply once to the parent with `await agent_message.send(message, receiver_role='parent')` giving the verdict, the attack.md path, the files you wrote, and the reasons in at most ten lines.
