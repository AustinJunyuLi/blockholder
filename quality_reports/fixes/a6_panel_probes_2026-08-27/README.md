# A6-continuity panel probes — 2026-08-27

Working probes and raw findings of the two opposed-brief Opus panellists on the re-derivation's
withheld change 6 (N11: continuity of 𝒯). Copied verbatim from the panel's scratchpad by the
orchestrator.

**Class: analysis-grade panel probes, not curated t2 checks.** These scripts were written and
executed by the panellists inside an adversarial investigation; they are filed so every number
quoted in the panel reports and the card's §5/§9 notes stays reproducible. They have not been
through the curation a `t2_*` check gets (frozen provenance block, single JSON verdict schema,
re-run verification). Follow-up on file: curate the decisive 𝒯₂-jump measurement (a6_A_probe4 /
a6_B_sweep) and the κ=0.15 node diagnosis (a6_B_node15) into proper t2 checks.

- `a6_A_*` — Analyst A (substantiate brief): `a6_A_findings.md` raw notes;
  probes 1–8 (type-mass census, 𝒯-jump, U-jump, precise two-sided limits, collapse face,
  channel decomposition, robustness verification, A3 open-set).
- `a6_B_*` — Analyst B (defuse brief): `a6_B_findings.md` raw notes (its §6 carries B's proposed
  §9 text); `struct` (locus characterisation), `sweep` (𝒯₂ across n(s) edges), `baseline`
  (collapse-face cleanliness), `chamber` (chamber-interior Θ⁺ rescue), `node15` (κ=0.15
  UNRESOLVED-node diagnosis), `resid`/`alt`/`argmax` (residual reproduction, alternatives,
  argmax reversal).

Panel reports (the adjudicated record): `research/model_v4/threads/2026-08-27_A6_panel_substantiate.md`
and `..._defuse.md`. Adjudication: card §5 A6/A3 evidence notes + §9 item 4;
`quality_reports/session_logs/2026-08-27_follow-ups.md`.
